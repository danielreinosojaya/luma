# Database with Prisma Accelerate (recommended for serverless/edge)
# Format: prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=your-api-key-here"

# Alternative: Direct PostgreSQL connection (for traditional hosting)
# DATABASE_URL="postgresql://user:password@host:5432/luma"

# Email (Brevo SMTP)
BREVO_SMTP_HOST="smtp-relay.brevo.com"
BREVO_SMTP_PORT="587"
BREVO_SMTP_USER="your-brevo-email@brevo.com"
BREVO_SMTP_PASS="your-api-key"
BREVO_FROM_EMAIL="noreply@lumabeauty.com"
BREVO_FROM_NAME="Luma Beauty Studio"

# Auth
NEXTAUTH_SECRET="your-generated-secret-here"
NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_TRUST_HOST="true"

# Redis (Upstash for rate limiting)
UPSTASH_REDIS_REST_URL="https://your-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-token"

# App
NEXT_PUBLIC_APP_URL="https://yourdomain.com"
NODE_ENV="production"
